# pdfer

Минималистичная читалка PDF с локальным переводом EN→RU. Windows, Tauri 2.

Выделите предложение — рядом появится перевод. Alt+клик — перевод целого абзаца.
Одной кнопкой переводится вся книга: готовые страницы сохраняются на диск,
клавиша T переключает оригинал и перевод, процесс можно прервать и продолжить
в любой момент. Перевод выполняет локальная модель — без интернета и подписок.

## Возможности

- **Чтение**: непрерывная прокрутка, 1/2/авто колонок, зум с якорем в точке
  курсора, тёмная тема, оглавление и внутренние ссылки, история переходов
  (Alt+←/→), запоминание позиции по каждой книге
- **Перевод выделенного и абзацев** (Alt+клик) — локальная модель
  HY-MT1.5-7B через llama.cpp, с контекстом абзаца
- **Перевод всей книги** — фоновый ран с паузой и продолжением; готовые
  страницы сохраняются и открываются офлайн, вёрстка абзацев и фигуры
  оригинала сохраняются
- **Глоссарий книги** — характерные термины добираются из текста
  статистически, переводятся моделью и применяются при переводе; правится
  вручную
- **Поиск по книге** (Ctrl+F) — по оригиналу и по готовому переводу
- **Командная палитра** (Ctrl+K): команды, «страница N», переключение книг,
  запуск поиска; оверлей клавиш — `?`
- **Библиотека** с обложками, прогрессом чтения и живым сканом папки
- **«Спросить»** (Ctrl+J) — вопросы по книге; использует Claude Code
  (опционально, требует установленный `claude` и интернет)

## Приватность

Книги, переводы и глоссарии не покидают компьютер: модели скачиваются один
раз и работают локально. Единственное исключение — опциональная функция
«Спросить»: вопрос и фрагмент открытой книги отправляются в Claude по явному
действию.

## Модели

| Модель | Роль | Размер | Лицензия |
| --- | --- | --- | --- |
| [HY-MT1.5-7B](https://huggingface.co/tencent/HY-MT1.5-7B-GGUF) (Tencent) | перевод EN→RU | 4,6 ГБ | Hunyuan Community License — бесплатно, включая коммерческое использование; **не действует в ЕС, Великобритании и Южной Корее** |
| [Qwen3.5-4B](https://huggingface.co/unsloth/Qwen3.5-4B-GGUF) (Alibaba) | перевод терминов глоссария (опционально) | 2,7 ГБ | Apache-2.0 |

Скачивание — только по явному действию пользователя, с лицензией на виду;
докачка продолжается с места обрыва и переживает перезапуск приложения.

## Сборка

Требуется: Windows 10/11, Node.js 20+, Rust (stable), инструменты сборки MSVC.

```sh
npm install
npm run tauri dev     # разработка (Vite + HMR)
npm run tauri build   # релизная сборка и инсталлятор
```

Инференс выполняет `llama-server.exe` (llama.cpp). В инсталлятор он входит
как ресурс: перед сборкой скрипт `npm run stage-llama` (вызывается
автоматически из `beforeBuildCommand`) копирует `llama-server.exe` и его
DLL из `%APPDATA%\com.stas.pdfer\llama\` (источник переопределяется
переменной `PDFER_LLAMA_SRC`) в gitignored-папку `llama-bin/`, откуда они
попадают в ресурсы приложения (`<resources>\llama\`). Приложение сначала
ищет `llama-server.exe` среди своих ресурсов, затем — в
`%APPDATA%\com.stas.pdfer\llama\` (дев-режим и ручные установки). Веса
моделей всегда в `%APPDATA%\com.stas.pdfer\models\` — их скачивает само
приложение.

Иконка генерируется из `src-tauri/icons/icon.svg`:

```sh
npx tauri icon src-tauri/icons/icon.svg
```

## Обновления

Автообновление намеренно **выключено**: манифесту апдейтера нужен хостинг
(решение Р-7 — статический манифест на своём домене), которого пока нет.
Когда домен появится, включение — три шага:

1. `src-tauri/Cargo.toml`: добавить `tauri-plugin-updater = "2"`, в
   `src-tauri/src/lib.rs` — `.plugin(tauri_plugin_updater::Builder::new().build())`.
2. `src-tauri/tauri.conf.json` — секция плагина (ключи генерирует
   `npx tauri signer generate`; приватный ключ в CI-секрет, ни в коем случае
   не в репозиторий):

   ```jsonc
   // добавить в корень конфига:
   "plugins": {
     "updater": {
       "pubkey": "<публичный ключ из tauri signer generate>",
       "endpoints": ["https://<домен>/pdfer/latest.json"]
     }
   }
   ```

3. `latest.json` на домене — статический манифест формата updater v2
   (version, notes, pub_date, platforms."windows-x86_64".{signature, url});
   собранный `*-setup.exe` подписывается тем же ключом при сборке.

Подпись кода (SmartScreen) — отдельная задача владельца (решение Р-4):
сертификат покупается, `certificateThumbprint`/`signCommand` добавляются в
`bundle.windows` конфига.

## Технологии

Tauri 2 · React 19 · TypeScript · Tailwind CSS 4 · PDF.js · llama.cpp

Версии и лицензии сторонних компонентов — в приложении: «Перевод ▾» → «О pdfer».
